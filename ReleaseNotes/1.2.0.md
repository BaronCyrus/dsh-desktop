# DSH Desktop 1.2.0

- 加入“检查更新…”菜单，使用 Sparkle 2 安全检查、下载并安装后续版本。
- 更新包使用独立 Ed25519 密钥签名，并通过 GitHub Releases 提供 appcast。
- 构建流程会嵌入并逐层签名 Sparkle 运行组件。

从 1.1.0 升级到 1.2.0 仍需手动覆盖安装一次；安装 1.2.0 后即可从应用菜单检查后续更新。

本版本采用免费 ad-hoc 签名，未经过 Apple 公证。从 GitHub 首次下载安装时，macOS 会要求在“系统设置 → 隐私与安全性”中手动确认打开。
